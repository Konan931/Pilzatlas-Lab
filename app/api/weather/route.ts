import { NextRequest } from 'next/server';
export const runtime='nodejs';
const valid=(v:string|null,min:number,max:number)=>{const n=Number(v);return v!==null&&Number.isFinite(n)&&n>=min&&n<=max?n:null};
export async function GET(req:NextRequest){const lat=valid(req.nextUrl.searchParams.get('lat'),51.2,53.8),lon=valid(req.nextUrl.searchParams.get('lon'),11,15.8);if(lat==null||lon==null)return Response.json({error:'invalid Berlin-Brandenburg coordinate'},{status:400});const p=new URLSearchParams({latitude:String(lat),longitude:String(lon),timezone:'Europe/Berlin',models:'icon_seamless',past_days:'7',forecast_days:'7',current:'temperature_2m,precipitation',daily:'precipitation_sum,temperature_2m_min,temperature_2m_max,et0_fao_evapotranspiration'});const r=await fetch(`https://api.open-meteo.com/v1/forecast?${p}`,{next:{revalidate:900}});if(!r.ok)return Response.json({error:'weather upstream unavailable'},{status:502});return new Response(await r.text(),{headers:{'Content-Type':'application/json','Cache-Control':'public, max-age=60','CDN-Cache-Control':'public, s-maxage=600'}})}
