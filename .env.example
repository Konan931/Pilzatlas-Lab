# Optional MapLibre-compatible style URL. The default is OpenFreeMap Liberty and needs no key.
NEXT_PUBLIC_MAP_STYLE_URL=

# Future opt-in Supabase sync. Leave unset for local-only field notes.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
