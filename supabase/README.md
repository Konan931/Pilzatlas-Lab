# Supabase / PostGIS preparation

No hosted Supabase project is required. Exact field coordinates remain local by default. Future authenticated sync uses PostGIS and Row Level Security; never expose service-role secrets in the browser and never publish exact coordinates by default.
